# Changelog

All notable changes to this crate are documented in this file. Format follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/); versions adhere to
[Semantic Versioning](https://semver.org/spec/v2.0.0.html).

The release workflow reads the section matching the git tag's version
(`backbone-search-v<version>`) and uses it as the GitHub Release body. If no
matching section is found it falls back to `## [Unreleased]`.

## [Unreleased]

## [2.0.0]

- Initial release as an independent crate (extracted from monorepo-backbone).
